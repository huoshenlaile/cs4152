#ifndef __NET_CONTROLLER_H__
#define __NET_CONTROLLER_H__

// This is actually the file we put netEvent. The netController itself is in CUGL, we need to extend a few events.
#endif /* NetController_h */
